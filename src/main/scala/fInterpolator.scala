object fInterpolator extends App{

  val pi: Double = 3.141592653589793
  println(f"Pi to 2 decimal places is $pi%.2f")
  println(f"Pi to 4 decimal places is $pi%.4f")
  //println(f"Pi to 2 decimal places is $pi%i") produces an error
  //because it is not an integer, is defined as floating-point.

  //Common format strings include
  // %f - Floating point number
  // %d - Decimal number
  // %i - int etc.

}
